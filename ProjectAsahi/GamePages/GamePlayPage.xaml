<Page
    x:Class="ProjectAsahi.GamePlayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectAsahi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark">

    <SwapChainPanel>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Click="{x:Bind Save}" Content="Save" Margin="4" Padding="4"/>
            <Button Click="{x:Bind Load}" Content="Load" Grid.Column="1" Margin="4" Padding="4" />
            <Button Click="{x:Bind BackLog}" Content="BackLog" Grid.Column="2" Margin="4" Padding="4" />
            <Button Click="{x:Bind Back}" Content="Title" Grid.Column="3" Margin="4" Padding="4"/>
        </Grid>

    </SwapChainPanel>
</Page>
